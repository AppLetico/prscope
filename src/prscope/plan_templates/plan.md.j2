# {{ session.title }}

Status: {{ session.status }}
Session: `{{ session.id }}`
Repo: `{{ repo.name }}`
Author model: `{{ session.author_model or "default" }}`
Critic model: `{{ session.critic_model or "default" }}`

{% if session.seed_type == "upstream_pr" and session.seed_ref %}
## Upstream PR Seed

Seeded from: `{{ session.seed_ref }}`
{% endif %}

## Executive Summary

{{ session.requirements }}

## Runtime Configuration Snapshot

- Author model: `{{ session.author_model or "from planning config" }}`
- Critic model: `{{ session.critic_model or "from planning config" }}`
- Issue dedupe: semantic-first (when enabled) with lexical fallback policy from `planning.issue_dedupe`

## Goals

<!-- Fill from plan body -->
{{ plan.plan_content }}

## Acceptance Criteria

- [ ] Plan approved
- [ ] Plan reviewed

## After approval (handoff)

Prscope **does not modify your repository**. This export is the durable artifact: share `PRD.md`, review in your editor, then implement using your normal workflow (branch, code review, CI).

Typical next steps:

1. Export or copy this `PRD.md` into your task tracker or design review.
2. Implement changes in your IDE; run your project’s tests and lint locally.
3. Open a pull request when ready.

## Suggested verification

Run checks **outside** Prscope that match your stack. Replace the examples below with commands from your repo (see `CONTRIBUTING.md`, `Makefile`, or `package.json`).

{% if critic_acceptance_bullets %}
Critic-stated criteria (from the latest structured review, when available):

{% for line in critic_acceptance_bullets %}
- {{ line }}
{% endfor %}

{% endif %}
Examples teams often use:

- `make check` or `make ci` (if your repo defines them)
- `pytest -q` or `npm test` (pick what your project documents)
- `ruff check .` / `eslint` / `tsc --noEmit` (as appropriate)

If a command is slow, run the narrowest check that still validates your change.
